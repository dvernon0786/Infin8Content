# Source Tree Analysis - Infin8Content

Generated: 2026-02-28
Scope: **Exhaustive Deep Scan**

## Monorepo Map

```
Infin8Content/
├── infin8content/          # Main Web Platform (Next.js 16)
│   ├── app/                # App Router (API, Dashboard, Onboarding)
│   ├── components/         # Design System & UI Library
│   ├── lib/                # Core Services & Business Logic
│   ├── hooks/              # Custom React Hooks
│   ├── types/              # TypeScript Definitions
│   ├── styles/             # Tailwind 4 CSS
│   └── __tests__/          # Vitest & Playwright Suites
├── tools/                  # Developer Tooling
│   └── eslint-plugin-design-system/ # Design compliance rules
├── _bmad/                  # BMad Agentic Framework
│   ├── core/               # Engine core
│   └── bmm/                # Business Management Methodology
├── supabase/               # Database Infrastructure
│   └── migrations/         # 75+ PostgreSQL Migrations
└── docs/                   # Exhaustive Documentation
```

## Deep Dive: infin8content

### 1. `app/` (Application Layer)
-   `api/`: REST endpoints. Follows the **Trigger API** pattern where controllers exclusively handle state-mutating requests.
-   `dashboard/`: The single-pane-of-glass dashboard. Purely reactive; no local state mutation of article lifecycle.
-   `onboarding/`: Wizard-based setup for organizations and research profiles.

### 2. `lib/` (Service Layer)
-   `services/`:
    -   `bulk-operations.ts`: **Lock Authority** for multi-article actions.
    -   `content-generation.ts`: Orchestrates LLM section-by-section writing.
    -   `seo-service.ts`: Integration with SEO analysis tools.
-   `inngest/`:
    -   `client.ts`: Event bus configuration.
    -   `functions/`: **Execution Authority**. Pure processors for generation, cleanup, and scheduling.
-   `supabase/`: DB clients, RLS helper functions, and auth wrappers.

### 3. `components/` (UI Layer)
-   `ui/`: Base Radix/Shadcn primitives.
-   `dashboard/`: Complex components like `BulkActionsBar` and `ArticleStatusList`.
-   `articles/`: Specialized displays like `VisualStatusIndicator`.
-   `mobile/`: Touch-optimized layouts for on-the-go monitoring.

### 4. `supabase/migrations/`
-   Deterministic database evolution. Notable migration: `20260226090000_normalize_article_schema.sql` (implements flattened state and org-based RLS).

---
*Generated by Infin8Content Documentation Engine.*
