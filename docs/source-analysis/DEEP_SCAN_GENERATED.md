---
description: Auto-generated deep scan summary (based on repository inspection)
---

# Deep Scan — Infin8Content

**Generated:** 2026-04-10

## Executive Summary

Repository type: Web (Next.js) application using TypeScript, React, and server/API routes.

This scan confirms a production-grade AI content platform architecture with a deterministic FSM, background workers (Inngest), Supabase-backed schema and migrations, and multiple external integrations (OpenRouter, Tavily, DataForSEO, WordPress, Stripe, Brevo).

## Key Findings

- Frameworks & tooling: Next.js, React, TypeScript, Tailwind, Storybook present; testing via Vitest and Playwright.
- State machine: Canonical FSM implementation in `infin8content/lib/fsm/` (atomic transitions enforced).
- Background processing: Inngest is used for event-driven workers (multiple intent.* events detected).
- Database: Supabase/Postgres with a large set of SQL migrations under `infin8content/supabase/migrations/`.
- Integrations: OpenRouter, Tavily, DataForSEO, WordPress adapters and other publisher connectors are implemented in `lib/services`.
- CI/CD: multiple workflows under `.github/workflows/` (CI, tests, visual-regression, performance, etc.).
- Secrets exposure: an `.env.local` file exists (`infin8content/.env.local`) containing API keys and service credentials — treat as sensitive and rotate immediately.

## Notable Paths & Examples

- FSM and engine: `infin8content/lib/fsm/workflow-fsm.ts`, `workflow-machine.ts`, `workflow-events.ts`
- Services & integrations: `infin8content/lib/services/intent-engine/*`, `infin8content/lib/services/openrouter/openrouter-client.ts`, `infin8content/lib/services/tavily/*`, `infin8content/lib/services/dataforseo/*`, `lib/services/publishing/wordpress-adapter.ts`
- Background workers & events: `lib/inngest/functions/*`, Inngest events such as `intent.step4.longtails`, `intent.step5.filtering`, `intent.step6.clustering`, `intent.step7.validation`, `intent.step8.subtopics`, `intent.step9.articles`.
- Migrations: `infin8content/supabase/migrations/*` (many timestamped SQL files)
- Tests & tooling: `infin8content` package.json contains `vitest`, `playwright`, `storybook` scripts.
- CI workflows: `.github/workflows/ci.yml`, `ts-001.yml`, `sm-validation.yml`, `visual-regression.yml`, etc.

## Security & Risks

- Sensitive keys detected in `infin8content/.env.local` (OpenRouter, Tavily, DataForSEO, Inngest keys, Supabase DB URL/keys). ACTION: remove from repo, add to secret manager, and rotate keys immediately.
- Recommend adding a secret-scanning step to CI and a pre-commit hook that prevents committing `.env*` files.

## Documentation Gaps (recommended)

- Formal API spec (OpenAPI / GraphQL schema) for server routes under `app/api/` or `pages/api/`.
- Data model documentation: map `supabase/migrations/` to canonical database schema docs and entity diagrams.
- FSM documentation: extract states & transition matrix from `lib/fsm/` into a developer-friendly diagram and canonical state machine doc.
- Async/event flow: document Inngest event names, payload shapes, worker responsibilities, and retry/poison-handling policies.
- Component catalog: link Storybook stories to the `components/` inventory.

## Actionable Next Steps

1. Rotate credentials found in `infin8content/.env.local` and remove the file from source control.
2. Add secret scanning to CI and a `.gitignore` rule for local env files.
3. Generate and commit:
   - OpenAPI spec for API routes
   - Data model reference (DB ERD + migration inventory)
   - FSM diagram + canonical transition table
   - Async/event inventory (Inngest events and worker mapping)
4. Add a `docs/README.md` (if missing) linking the new artifacts and explain update process.
5. Optionally open a branch `docs/deep-scan-2026-04-10` with these docs and a PR for review.

## Where I looked (examples)

- `infin8content/package.json` (scripts + dependencies)
- `infin8content/.env.local` (sensitive values — redacted)
- `infin8content/lib/fsm/` and `lib/services/intent-engine/`
- `infin8content/supabase/migrations/`
- `.github/workflows/`
- `docs/project-documentation/DEEP_CODEBASE_ANALYSIS.md` and top-level `DEEP_SCAN_REPORT.md` for style and coverage

---
Generated by an automated deep-scan assistant. Tell me which sections you'd like expanded into separate `docs/` artifacts (API spec, FSM diagram, DB model, event inventory), and I will generate them and open a PR.
